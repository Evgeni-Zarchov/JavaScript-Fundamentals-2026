function requiredReading(numberOfPage, pages, days) {
    let time = numberOfPage / pages;

    let totalTime = time / days;

    console.log(totalTime);
}

requiredReading(
    212,
    20,
    2);
requiredReading(
    432,
    15,
    4);